# Markup: **bannerRight**
- marca un bloque como `bannerRight` el cual será visible cuando se abra las demás páginas del documento.
- este bloque se define en la primera página del documento o `bannerRightTemplate` como un parámetro del documento.


Notas:
- por el momento el bannerRight funciona únicamente cuando se manejan tabs verticales.
