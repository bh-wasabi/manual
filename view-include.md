# Objeto: **include**
- incluye un o varios campos a la consulta
- si se usa el `include` el sistema ignora `exclude`.

## Parámetros
##### field
- ruta del campo dentro del documento, normalmente es "seccion.campo"
