# Objeto: **config**
- define un `preset` fijo del sistema
- es posible agregar otros campos como en los otros preset's.

## Parámetros
#### id
- identificador del `preset`

#### nombre
- nombre del `preset`

## Sub objetos
#### item
- debe contener por lo menos `id` y `nombre`
